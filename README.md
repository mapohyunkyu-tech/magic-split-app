# MAGIC V27 RECOVERY CANDIDATE THRESHOLD FIX

`v27_HOLDING_ACTION_FOCUS_UI_20260618` 기반 안정 패치입니다.

## 수정 이유

회수모드에서 `차수손익률 >= -2%`이면 `회수후보`로 판정되면서,
아래께 산 종목처럼 +2~3% 약수익 종목도 상단 `회수/익절검토` 표에 올라오는 문제가 있었습니다.

예: 이수화학 +2.89% → 기존에는 `🔵 회수/익절검토` 표시

## 수정 내용

- `회수/익절검토` 상단표는 실질 수익권만 표시
  - `보유판정=익절검토` 또는
  - `차수손익률 >= 5%` 또는
  - `종목전체손익률 >= 5%`
- +5% 미만 본전권/약수익 회수후보는 `⚪ 보유관망`으로 표시
- `현재장판단` 문구도 `약수익/본전권 보유관망`으로 변경
- 추가매수 기준가/마지막 차수 판단/고정 종목 컬럼 등 기존 기능 유지

## 사용법

GitHub 루트에 `app.py`, `requirements.txt`, `runtime.txt`를 올리세요.
Streamlit Cloud는 재부팅 또는 Clear cache 후 실행하세요.
